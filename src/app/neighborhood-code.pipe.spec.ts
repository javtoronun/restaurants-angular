import { NeighborhoodCodePipe } from './neighborhood-code.pipe';

describe('NeighborhoodCodePipe', () => {
  it('create an instance', () => {
    const pipe = new NeighborhoodCodePipe();
    expect(pipe).toBeTruthy();
  });
});
